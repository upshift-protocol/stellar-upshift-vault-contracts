#!/usr/bin/env bash
set -euo pipefail

echo "Running pre-commit checks..."

# Check Rust formatting
echo "Checking formatting..."
cargo fmt --all --check

# Run clippy lints
echo "Running clippy..."
cargo clippy -p august-vault --all-targets -- -D warnings

echo "Pre-commit checks passed."
