# Expected SHA-256 of each contract's attested, reproducible release WASM
# (target wasm32v1-none, Rust 1.95.0, Stellar CLI 25.1.0, --meta source_repo).
#
# The `reproducible-build` CI job rebuilds each contract with that pinned
# toolchain and asserts the hash below. When a contract's bytecode changes
# intentionally, update the matching hash here in the SAME PR — the diff makes
# the bytecode change explicit and reviewable, and CI fails if the file and the
# built bytes disagree.
#
# Reproduce locally: see VERIFY.md § B.
#
# package       sha256
august-vault    3bd05dfa2bf65299d359e86a4672a45900e8c5768d9f056ad8da5ccd779bcd12
xlm-strategy    e3443e37b6da76ef061051f5e170537ef6e596711869c09a1ec9cd9e55345da4
